import Foundation

// Replace the identifier with your real App Group ID (e.g., "group.com.yourcompany.clipboard")
// Make sure this file is included in BOTH targets: the main app and ShareLinkExtension.

enum AppGroup {
    static let identifier = "group.com.tamaraosseiran.clipboard"
}

enum SharedKeys {
    static let inbox = "SharedURLInbox"
}
